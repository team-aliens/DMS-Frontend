import { Apis } from '@dms/apis';
import { Components } from '@dms/components';
import { Router } from './router';

function App() {
  console.log(Apis());
  console.log(Components());
  return <div />;
}
